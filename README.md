# ATR21 LOG — Landing Page

Landing page da ATR21 LOG (versão premium com animações).

## Arquivos
- `index.html`: versão principal (premium)
- `index-premium.html`: redireciona para `index.html` (mantido só por compatibilidade)

## Rodar localmente
```bash
python3 -m http.server 8000
```
Abra:
- `http://localhost:8000/`
